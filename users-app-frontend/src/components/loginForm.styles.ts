import { SxProps, Theme } from '@mui/material';

export const loginBoxStyle: SxProps<Theme> = {
  maxWidth: 400,
  mx: 'auto',
  mt: 4,
  p: 3,
  borderRadius: 2,
};

export const formTitleStyle: SxProps<Theme> = {
  textAlign: 'center',
  fontWeight: 600,
};

export const newUserButtonStyle: SxProps<Theme> = {
  mt: 2,
  textTransform: 'none',
  fontSize: '0.9rem',
  color: '#1976d2',
  textAlign: 'center',
  width: '100%',
};
